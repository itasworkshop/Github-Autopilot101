1. Take a shared git repo 
	- git pull
	- git clone
	- git checkout
once commit in main branch and once commit in feature branch
raise pull request and add me reviewer.

2 Perform usefull commands as shown during demo

3 Create a new commit in your feature after rebasing the feature branch then check commit log seqence.

 git rebase -i main
